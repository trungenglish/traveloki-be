// import express from 'express'
// import { authController } from '../controllers/account.controller'
// import { asyncHandler } from '@/common/middlewares/asyncHandler.middeware'

// export const routerAccount = express.Router()

// routerAccount.post('/auth/register', asyncHandler(authController.register))
// routerAccount.post('/auth/login', asyncHandler(authController.login))
